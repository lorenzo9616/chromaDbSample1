# Project Goal

Interactive ChromaDB + .NET Blazor sample application with Docker deployment.

## Overview

This project is a learning sample that demonstrates how to integrate ChromaDB (a vector database) with a .NET Blazor Web App. It provides an interactive UI for seeding documents and performing semantic similarity searches against a ChromaDB instance, all orchestrated via Docker Compose.
